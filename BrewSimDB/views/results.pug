extends layout

block content
    .text-center Homebrewers DB Query Results
    table#table
        thead
            tr
                th
                    
        tbody
            if print.length>0
            each item, i in print
                tr
                    to
                        each attribute, j in item
                            = j + ": " + attribute + "  |  "
                        br